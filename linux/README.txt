Sweitzer Automations 3-22-26 — Linux
====================================

User-friendly ways to run the same dashboards as on Windows (browser + optional Tk window).

Read the full guide: ../LINUX.md  (repo root)

Quick start (from repo root):
  ./linux/run_dashboards.sh       — desktop launcher (needs: python3-tk)
  ./linux/serve_dashboards.sh     — browser only, no Tk
